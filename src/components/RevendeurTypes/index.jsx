import React from 'react';
import { ScrollView, View } from 'react-native';
import UberTypeRow from '../../components/UberTypeRow'

const RevendeurType = () => {
    return (
        <ScrollView>
            <View>
            <UberTypeRow />
            </View>
        </ScrollView>
    );
};

export default RevendeurType;